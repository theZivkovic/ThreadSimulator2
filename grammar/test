mutex mu

int func(int a, int b)
{
	mu.lock()
		int x = a
		int y = b
	mu.unlock()
	
	return x + y * 2
}

int main(int argc, int argv)
{
	thread t[100]

	for (int i = 0; i < 10; i = i + 1)
	{
		int x

		x = x + 2
	}
	t[i].join()
}